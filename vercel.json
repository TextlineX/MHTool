{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/wake", "destination": "/" },
    { "source": "/wake/:path*", "destination": "/" }
  ],
  "headers": [
    {
      "source": "/wake",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
